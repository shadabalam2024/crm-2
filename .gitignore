node_modules/
dist/
.DS_Store

.env

# Supabase CLI local state (project ref, pooler host) — not project source
supabase/.temp/

# throwaway test-harness scripts (see scripts/launch-test-app.mjs)
__test_*.mjs
__test_*.js

# License signing key — never commit. Losing/leaking this lets anyone mint
# valid license keys for any machine. See tools/generateKeypair.js.
tools/private-key.pem

# local Claude Code session/scheduler state, not project source
.claude/
.vercel
.env*
